# implement-breakout-foundation

Implement the first safe breakout strategy slice: configuration contracts, canonical market data models, provider abstractions, normalization, level detection, setup scoring, and no-lookahead tests. Live broker execution, UI, production ops, and full backtesting runtime remain out of scope.
