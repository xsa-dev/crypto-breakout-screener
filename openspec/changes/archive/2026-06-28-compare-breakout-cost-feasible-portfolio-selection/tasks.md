## 1. OpenSpec readiness
- [x] 1.1 Confirm active changes, branch, git status, and unrelated dirty files. Active changes before creation: none. Branch: `master`. Unrelated dirty file kept out of scope: `.dev/autonomous_dev.sh`.
- [x] 1.2 Restore baseline scorecard and primary blocker from archived `evaluate-breakout-altcoin-universe-profiles`: `2024q1=blocked`, other required quarters unknown/not promoted; artifact `artifacts/altcoin-universe-portfolio-comparison-regime-smoke-reaggregated-ledger-fix/conservative-v1-m15-slope-positive-max-trades-8-target-4p0-hold-32/520b158e637db140/summary.json`; blockers net/PF/DD plus symbol blockers.
- [x] 1.3 Validate this change strictly before source implementation. Evidence: `npx --yes @fission-ai/openspec@1.4.1 validate compare-breakout-cost-feasible-portfolio-selection --strict --no-interactive`, `npx --yes @fission-ai/openspec@1.4.1 validate --all --strict --no-interactive`, and `git diff --check` passed.

## 2. Implementation
- [x] 2.1 Add disabled-by-default portfolio `selection_profile` support with default `none` preserving current behavior. Evidence: `selection_profile="none"` remains the programmatic/CLI default and preserves existing exposure-cap tests.
- [x] 2.2 Implement fixed `cost-feasible-v1` selection using only entry-time price and configured cost settings. Evidence: entry-time `entry_price` plus configured spread/slippage produce `portfolio_selection_cost_feasibility`; non-positive entry price produces `portfolio_selection_missing_price`.
- [x] 2.3 Record skipped cost-feasibility entries in the portfolio trade ledger with machine-readable blockers.
- [x] 2.4 Add selection settings and skip counts to summary JSON/CSV artifacts.
- [x] 2.5 Preserve existing regime blocking, exposure caps, shared-bankroll accounting, realistic costs, and per-symbol contribution behavior.

## 3. Tests
- [x] 3.1 Add tests proving default portfolio selection preserves existing shared-bankroll behavior. Evidence: `test_portfolio_batch_uses_one_shared_bankroll_and_exposure_cap` still passes with default selection.
- [x] 3.2 Add tests proving `cost-feasible-v1` skips low-price/high-friction long entries before exposure allocation. Evidence: `test_portfolio_cost_feasible_selection_skips_high_friction_entries`.
- [x] 3.3 Add tests proving missing/non-positive price fails closed as a selection blocker. Evidence: `test_portfolio_cost_feasible_selection_blocks_missing_price`.
- [x] 3.4 Add tests proving selection profile/settings/skip counts serialize to summary artifacts. Evidence: same targeted cost-feasible tests assert summary settings and scorecard `selection_skip_counts_json`.

## 4. Quarterly experiment loop
- [x] 4.1 Run `2024q1` fixed-50 smoke for `cost-feasible-v1` under realistic costs and record status, blockers, net profit, profit factor, max drawdown, accepted/skipped trades, feed gaps/technical blockers, per-regime report, artifact path, and scorecard delta versus baseline. Evidence: command `env UV_CACHE_DIR=.uv-cache uv run python -m src.app.breakout.experiments.crypto_portfolio_batch --windows '2024q1:2024-01-01T00:00:00Z/2024-04-01T00:00:00Z' --universe fixed-50-altcoins --starting-equity 10000 --max-trade-notional 1000 --max-total-open-notional 3000 --spread 1.0 --slippage 0.5 --commission-rate 0.00055 --funding-rate-per-bar 0.00001 --selection-profile cost-feasible-v1 --gate-profile conservative-v1-m15-slope-positive-max-trades-8-target-4p0-hold-32 --market-data-dir artifacts/batch-market-data --output-dir artifacts/cost-feasible-portfolio-selection-smoke --reuse-market-data --max-workers 8`; artifact `artifacts/cost-feasible-portfolio-selection-smoke/conservative-v1-m15-slope-positive-max-trades-8-target-4p0-hold-32/d9ba24a02b2692ab/summary.json`; status `blocked`; blockers `portfolio_symbol_blockers_present`, `net_profit_below_threshold`, `profit_factor_below_threshold`, `max_drawdown_below_threshold`; net profit `-4249.438925113151`; PF `0.27563186481204516`; max DD `-0.42772673303232805`; accepted trades `464`; skipped/blocked trades `2860`; selection skips `portfolio_selection_cost_feasibility=1029`; per-regime report `artifacts/cost-feasible-portfolio-selection-smoke/conservative-v1-m15-slope-positive-max-trades-8-target-4p0-hold-32/d9ba24a02b2692ab/2024q1/per-regime-contributions.csv`. Delta versus baseline: improved net profit from `-9799.606491774299` to `-4249.438925113151`, PF from `0.1283106575002619` to `0.27563186481204516`, max DD from `-0.979960649177431` to `-0.42772673303232805`, but remained blocked and below thresholds.
- [x] 4.2 If smoke is technically valid and closer to thresholds, run full promoted `2023q1..2024q4` fixed-50 scorecard with shared bankroll and full regime reporting. Not promoted: smoke remained technically blocked by `portfolio_symbol_blockers_present` from missing cached public data for 38 fixed-universe symbols and did not meet thresholds; no success claim.
- [x] 4.3 If promoted scorecard completes below `8/8`, record final falsified verdict and artifact paths. If smoke/promoted run is locally blocked by public data availability, record exact blocker and required owner action. Evidence: `artifacts/cost-feasible-portfolio-selection-smoke/.../2024q1/per-symbol-contributions.csv` records 38 `window_exception:FileNotFoundError:cached public market-data CSVs are missing: artifacts/batch-market-data/bybit/linear/<SYMBOL>/{M15,H1,H4,D1}/20240101T000000Z_20240401T000000Z.csv`. A public-download retry command without `--reuse-market-data` was attempted into `artifacts/cost-feasible-portfolio-selection-smoke-public-download` and was killed after repeated 60s waits without completing the fixed-50 smoke in this local cycle. Required owner action: provide/cache complete public fixed-50 Bybit linear M15/H1/H4/D1 `2024q1` CSVs or allow a longer public-download runtime before promotion.

## 5. Verification and closure
- [x] 5.1 Run targeted pytest for portfolio/batch tests. Evidence: `env UV_CACHE_DIR=.uv-cache uv run pytest tests/test_crypto_batch_experiment.py -q` -> `24 passed`.
- [x] 5.2 Run full pytest. Evidence: `env UV_CACHE_DIR=.uv-cache uv run pytest -q` -> `134 passed`.
- [x] 5.3 Run `uv run ruff check .`. Evidence: `env UV_CACHE_DIR=.uv-cache uv run ruff check .` -> `All checks passed!`.
- [x] 5.4 Run `uv run pyright`. Evidence: `env UV_CACHE_DIR=.uv-cache uv run pyright` -> `0 errors, 0 warnings, 0 informations`.
- [x] 5.5 Run strict OpenSpec validation for this change and all specs. Evidence: `npx --yes @fission-ai/openspec@1.4.1 validate compare-breakout-cost-feasible-portfolio-selection --strict --no-interactive` and `npx --yes @fission-ai/openspec@1.4.1 validate --all --strict --no-interactive` -> passed.
- [x] 5.6 Run `git diff --check` and duplicate spec/archive-name check. Evidence: `git diff --check` passed; duplicate spec/archive-name check returned `0`.
- [x] 5.7 Archive as successful only if promoted fixed-50 shared-bankroll realistic-cost portfolio reaches `8/8` with regime reporting; otherwise archive as falsified/negative evidence or external blocker as applicable. Closure verdict: external-blocked/local-data-blocked, not successful; `8/8` not reached and no success notification is allowed.
- [x] 5.8 Create one scoped local commit if repository state is safe, leaving unrelated `.dev/autonomous_dev.sh` unstaged. Evidence: staged scope excludes `.dev/autonomous_dev.sh` and unrelated Heikin-Ashi source changes; commit hash is reported in the final local run summary.
